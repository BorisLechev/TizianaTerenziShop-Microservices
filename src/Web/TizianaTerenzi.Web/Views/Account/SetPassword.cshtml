@model UserSetPasswordInputModel

@{
    ViewData["Title"] = "SetPassword";
}

<div class="text-center w-75 w-md-50 mx-auto">
    <h2>@ViewData["Title"]</h2>
    <p>Once you set a password, you will be able to log in using your email and password instead of using Facebook and Google.</p>
    <hr />

    <form method="post">
        <div asp-validation-summary="All" class="text-danger"></div>

        <div class="form-group">
            <label>Email</label>
            <input class="form-control" value="@Model.Email" disabled />
        </div>
        <div class="form-group">
            <label asp-for="@Model.NewPassword"></label>
            <input class="form-control" asp-for="@Model.NewPassword" />
            <span asp-validation-for="@Model.NewPassword" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="@Model.ConfirmPassword"></label>
            <input class="form-control" asp-for="@Model.ConfirmPassword" class="form-control" />
            <span asp-validation-for="@Model.ConfirmPassword" class="text-danger"></span>
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-primary w-100 p-3">Set password</button>
        </div>
    </form>
</div>
