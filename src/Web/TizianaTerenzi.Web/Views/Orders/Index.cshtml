@inject IViewLocalizer Localizer
@model IEnumerable<OrdersListingViewModel>

@{
    this.ViewData["Title"] = Localizer["My orders"];
}

<div class="container" style="margin-top: 100px;">
    <div class="mx-auto my-4">
        <div class="text-center mb-3">
            <h2>@this.ViewData["Title"]</h2>
        </div>

        <table id="dataTable" class="table table-hover dt-responsive nowrap" style="width: 100%;">
            <thead>
                <tr class="font-weight-bold">
                    <th>@Localizer["Full name"]</th>
                    <th>@Localizer["Order Date"]</th>
                    <th>@Localizer["Total price"]</th>
                    <th>@Localizer["Discount code name"]</th>
                    <th>%</th>
                    <th>@Localizer["Status"]</th>
                    <th>@Localizer["Actions"]</th>
                    <th>PDF</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var order in Model)
                {
                    <tr>
                        <td>@order.UserFullName</td>
                        <td><time datetime="@order.CreatedOn.ToString("O")"></time></td>
                        <td>@order.TotalPrice</td>
                        @if (order.DiscountCodeName != null)
                        {
                            <td>@order.DiscountCodeName</td>
                        }
                        else
                        {
                            <td></td>
                        }
                        <td>@order.DiscountCodeDiscount</td>
                        <td>@order.StatusName</td>
                        <td>
                            <a asp-area="" asp-controller="Orders" asp-action="Products" asp-route-id="@order.Id" class="btn btn-outline-primary">
                                <i class="fas fa-pen"></i>
                                @Localizer["Products"]
                            </a>
                        </td>
                        <td>
                            <a asp-area="" asp-controller="Orders" asp-action="GeneratePdf" asp-route-orderId="@order.Id" class="btn btn-outline-success">
                                <i class="fas fa-file-pdf"></i>
                                PDF
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>